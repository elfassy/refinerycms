<nav id="menu">
  <% enable_tooltips = RefinerySetting.find_or_set(:enable_refinery_menu_tooltips, true)
  Refinery::Plugins.active.in_menu.each do |plugin| -%>
    <%= link_to plugin.title, plugin.url,
                :class => (plugin.highlighted?(params) ? "active" : ""),
                :id => "plugin_#{plugin.title}",
                :title => (enable_tooltips ? plugin.tooltip : nil) %>
  <% end -%>
  <% if Refinery::Plugins.active.in_menu.many? and RefinerySetting.find_or_set(:refinery_enable_backend_reordering, true) -%>
    <%= link_to refinery_icon_tag('arrow_switch.png'), "", :id => "menu_reorder", :title => "Reorder menu" %>
    <%= link_to refinery_icon_tag('tick.png'), "", :id => "menu_reorder_done", :title => "I'm done reordering the menu", :style => "display: none;" %>
  <% end %>
  <%= link_to "Log out", logout_url(:only_path => true), :id => "logout" %>
</nav>
<input type='hidden' id='admin_authenticity_token' value='<%= form_authenticity_token %>' />
